## Task 5 - Stateless API

A stateless API treats every request as independent. The server does not remember information from previous requests because of this, every request must include all the information needed to process it, such as authentication tokens or request data.

Stateless APIs are easier to scale, more reliable, and allow multiple servers to handle requests without sharing session data.